{
	"name": "hackerpremierleague",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"start": "node index.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
		"dev-client": "cd client && npm start",
		"dev-server": "nodemon",
		"postinstall_": "cd client && npm install"
	},
	"author": "Maneesh K",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"mongoose": "^5.11.5"
	},
	"devDependencies": {}
}
